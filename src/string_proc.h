/*
 * Author: Zhylko Barys
 *
 * Заголовочный файл модуля обработки строк.
 * Содержит интерфейс для работы со строками:
 * - удаление пробелов
 * - разделение строки по индексам
 */

#ifndef STRING_PROC_H
#define STRING_PROC_H

/*
 * Функция: remove_spaces
 * Назначение: удаляет все пробелы из входной строки
 * Входные параметры:
 *   src - исходная строка
 *   dst - буфер для результата (должен быть достаточного размера)
 * Выходные параметры:
 *   dst - строка без пробелов
 */
void remove_spaces(const char *src, char *dst);

/*
 * Функция: split_by_indices
 * Назначение: разделяет строку на две по четным и нечетным индексам
 * Входные параметры:
 *   src - исходная строка (без пробелов)
 *   even - буфер для символов с четными индексами
 *   odd - буфер для символов с нечетными индексами
 * Выходные параметры:
 *   even - строка из символов с четными индексами
 *   odd - строка из символов с нечетными индексами
 */
void split_by_indices(const char *src, char *even, char *odd);

#endif